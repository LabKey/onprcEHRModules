<customView xmlns="http://labkey.org/data/xml/queryCustomView" label="Active Grants">
    <sorts>
        <sort column="grantNumber" descending="false"/>
    </sorts>
    <filters>
        <filter column="enddateCoalesced" operator="dategte" value="-0d"/>
    </filters>
</customView>