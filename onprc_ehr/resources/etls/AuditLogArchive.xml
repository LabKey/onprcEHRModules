<?xml version="1.0" encoding="UTF-8"?>
<!--This ETL triggers the audit log archiving Audit Logs

 -->

<etl xmlns="http://labkey.org/etl/xml">

    <name>ArchiveAuditLogs</name>

    <description>Executes a Stored Procedure that reviews all Audit tables .</description>

    <transforms>

        <transform id="ArchiveAuditLogs" type="StoredProcedure">
            <description>Runs a Stored Procedure</description>
            <procedure schemaName="onprc_ehr" procedureName="ArchiveAuditTables">
            </procedure>
        </transform>

    </transforms>
    <schedule>
        <!--Runs at 4:30 each morning-->
        <cron expression="0 00 03 * * ?"/>
    </schedule>


</etl>
